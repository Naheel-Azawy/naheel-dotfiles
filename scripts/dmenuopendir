#!/bin/bash
D="$1"
open=$(echo -e "No\nTerminal\nRanger\nLF" | dmenu -i -p "Open $(basename $D)?")
cd "$D"
case "$open" in
    Ranger)   exec theterm ranger;;
    LF)       exec theterm lf;;
    Terminal) exec theterm;;
esac
